start.sync {let x + devyansh == main.17325.com.pubg.imobile.obb 
            if true;
            start.sync

            if false;
            start.lib
            let x = bgmi.lib 
const devyash = "main.17325.com.pubg.imobile.obb"
            
let recoil = 1-1
            if recoil === 0 
let recoil.Lib + devyansh = devyansh === lib

if x try Connection == lost reconnecting
    if Connection = true
            change.value try { recoil + devyansh + "obb is successfully working
